package model

// CTXKey is the type used as a context key for storing token.
type CTXKey string

const (
	TokenKey CTXKey = "token" // TokenKey is the specific key used in the context to store token.
)
